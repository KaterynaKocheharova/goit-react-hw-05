import { useState, useEffect } from "react";
import { getMovieCast } from "../../movies-api";

const MovieCast = () => {
  const [getMovieCastData, setMovieCastData] = useState(null);

  useEffect(() => {});

  return <div>Movie Cast</div>;
};

export default MovieCast;
